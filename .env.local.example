# ─── Supabase ───
# Get these from: supabase start (local) or your Supabase dashboard (prod)
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=<paste-anon-key-from-supabase-start>
SUPABASE_SERVICE_ROLE_KEY=<paste-service-role-key-from-supabase-start>

# ─── Auth ───
# IMPORTANT: Generate a real secret for production: openssl rand -base64 32
AUTH_SECRET=<run-openssl-rand-base64-32>
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ─── Cache ───
# IMPORTANT: Generate a real secret for production: openssl rand -hex 32
REVALIDATION_SECRET=<run-openssl-rand-hex-32>

# ─── Feature flag: use Supabase or static data ───
# Set to "true" to pull data from Supabase. Set to "false" (or omit) to use the old static data files.
NEXT_PUBLIC_USE_SUPABASE=true
